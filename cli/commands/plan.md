# CLI/Commands - Optimization Plan

<!-- SELF-DESTRUCT: rm -f plan.md when complete -->

## Tasks
- [ ] Merge all command JSON into commands.csv
- [ ] Use format: `id,name,args,desc`
- [ ] Remove individual JSON files after CSV migration

## Target: ~70% token reduction
